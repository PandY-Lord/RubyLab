<% @post.each do |post| %>
  <div class="alert">
    <h3><%= post.title %></h3>
    <p><%= post.description %></p>
    <%= link_to "Открыть полностью", post_path(post), :class => "btn btn-outline-primary" %>
  </div>

<% end %>